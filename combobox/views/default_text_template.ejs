<span style="float:left;display: block;height:13px;" 
      class="ui-icon <% if (item.type == "folder") {%>ui-icon-folder-open<% } else { %>ui-icon-document<% } %>">
</span>
<span <% if(item.enabled) { %>style="<%= textStyle %>"<% } else {%>class="<%= options.disabledClassName %><% } %>">
	<%= item.text %>
</span>
