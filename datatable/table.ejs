<table class="ui-widget">
	
	<thead>
		<tr class="ui-widget-header">
			
	<%  for(var f=0;f<fields.length;f++){ 
	       var field = fields[f]; %>			
		   <th field="<%= field.name %>"><%= field.text %></th>
     <% } %>
	 
		</tr>
	</thead>
		
	<tbody>
	<%  for(var r=0;r<rows.length;r++){ 
	       var row = rows[r]; %>
		   
		   <tr>
	   <%  for(var f=0;f<fields.length;f++){ 
	           var field = fields[f]; %>			   	
		
		   	   <td><%= row[field.name] %></td>
		
        <% } %>			   
		   </tr>
		   
     <% } %>		   
	</tbody>

</table>	
			
